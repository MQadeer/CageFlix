/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      // add any other image domains you use
    ],
  },
};

export default nextConfig;
